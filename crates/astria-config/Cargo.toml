[package]
name = "astria-config"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
serde = { workspace = true }
figment = { workspace = true, features = ["env"] }
once_cell = { workspace = true, optional = true }
regex = { workspace = true, optional = true }
astria-config-attribute = { path = "../astria-config-attribute", optional = true }

[features]
attribute = ["dep:astria-config-attribute"]
config-tests = ["figment/test", "dep:once_cell", "dep:regex"]
