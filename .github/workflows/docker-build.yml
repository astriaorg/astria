name: Docker

# Trigger on pushes to master branch, new semantic version tags, and pull request updates
on:
  workflow_dispatch:
  push:
    branches:
      - "release/sequencer-v0.8.x"
    tags:
      - "sequencer-v0.8.[0-9]+"
      - "sequencer-v0.8.[0-9]+-alpha.[0-9]+"
      - "sequencer-v0.8.[0-9]+-beta.[0-9]+"
      - "sequencer-v0.8.[0-9]+-rc[0-9]+"
  # trigger on pull request updates when target is `main` branch
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - labeled
    branches:
      - "release/sequencer-v0.8.x"

jobs:
  run_checker:
    uses: ./.github/workflows/reusable-run-checker.yml

  sequencer:
    needs: run_checker
    if: needs.run_checker.outputs.run_docker == 'true'
    uses: './.github/workflows/reusable-docker-build.yml'
    with:
      package-name: sequencer
      target-binary: astria-sequencer
    secrets: inherit

  docker:
    if: ${{ always() && !cancelled() }}
    needs: [sequencer]
    uses: ./.github/workflows/reusable-success.yml
    with:
      success: ${{ !contains(needs.*.result, 'failure') }}
